# AI 协同日志 (AI-Collaboration.md)

> 本文档如实记录本项目开发中与 AI（Claude）的协同过程。
> 课题评分允许使用 AI 生成代码，重点考查"把 AI 当协同研究员、而非盲目代写工具"的机器
> 思路深度，故本文据实记录分工、关键决策与一次真实的调试过程。

## 协作定位与分工

| 环节 | 主要承担方 | 说明 |
|---|---|---|
| 物理机制与算法架构 | 我（理解与把关） | BB84 原理、玻恩定则测量、QBER 推导、三层架构的逻辑均由我理解并能独立讲解 |
| 初版代码实现 | AI 生成 | 我给出课题要求后，由 AI 产出各模块的具体实现，我逐块审阅确认 |
| GitHub 工程搭建 | AI 指导 + 我执行 | 仓库创建、分批提交、CI 配置由 AI 给出步骤，我逐步操作完成 |
| 测试设计 | AI 建议 + 我采纳 | AI 提出"按物理性质分层断言"的测试策略，我理解后采用 |
| 调试与定位 | 共同完成 | 真实 CI 报错由我发现、与 AI 共同定位根因、我执行修复 |

---

## 真实协同记录

### #1 初版代码实现（AI 生成，我审阅把关）
- 我提供课题要求（Alice/Bob/Eve 三角色、对基筛选、截获-重发 QBER）后，由 AI 生成模块化实现。
- 关键设计：测量采用态矢量 + 玻恩定则 $P(k)=|\langle e_k|\psi\rangle|^2$ 的真实投影，
  而非"对基→正确 / 错基→50/50"的查表捷径，使 25% 误码率从物理中**涌现**。
  我对照自己对原理的理解逐块确认后保留。
- 架构：`qubit`（物理层）/ `parties`（角色层）/ `protocol`（编排层）三层解耦。

### #2 GitHub 工程从零搭建（AI 指导步骤，我执行）
- 我此前未使用过 GitHub。AI 给出"建仓库 → 分批提交 → 补 CI 与配置文件"的逐步操作指引。
- 我的决策：选择 Public 仓库（便于社区反馈）；按 `qubit → parties → protocol → tests → docs`
  的模块顺序分多次提交，体现循序渐进的开发过程，而非期末一次性上传。

### #3 CI 报错的定位与修复（核心条目）
- **现象**：GitHub Actions 连续两次失败，报 `ModuleNotFoundError: No module named 'bb84'`。
- **连续追问**：我把失败截图反馈给 AI，追问"本地能跑、为什么 CI 失败"。
- **逻辑重构（共同定位根因）**：CI 用的是裸命令 `pytest`，它不会把项目根目录加入 Python
  搜索路径，导致 `from bb84.protocol import ...` 找不到 `bb84` 包；而本地用 `python -m pytest`
  会自动加入根目录，因此本地能跑、CI 不能跑，掩盖了问题。
- **修复**：新增 `pyproject.toml` 并配置 `pythonpath = ["."]`，使裸 `pytest` 与
  `python -m pytest` 在任何环境（含他人 clone 后）都能跑通；提交后 CI 通过（绿勾）。
- **收获**：理解了 pytest 的模块导入机制，以及"可复现性"为何是开源工程的硬指标。

### #4 测试文件的设计思路（AI 建议，我采纳）
- 采用原则：测试断言的是"物理上必然成立的性质"，而非仅"代码能跑"。
- 分层：先测底层不变量（对基测量确定、错基 50/50、测后态坍缩），
  再测协议层性质（无 Eve → QBER=0 且密钥一致、筛选率 ≈ 50%、全程 Eve → ≈ 25%、半程 → ≈ 12.5%）。
- 工程细节：确定性事件用精确断言，统计性事件用区间断言 + 大样本 + 固定随机种子，
  兼顾严格性与稳定性（避免偶发性挂掉）。

### #5 文件组织修正
- 上传时误把 `.gitignore` 新建进了 `.github/workflows/` 子目录；意识到它应位于仓库根目录后，
  退回根目录重建。

---

## 我的把关与独立理解
- 我能用狄拉克符号独立讲解：互无偏基、玻恩定则测量、对基筛选，
  以及截获-重发攻击下 $\text{QBER}=\tfrac14$ 的逐情形推导（详见 `docs/PHYSICS.md`）。
- 我理解三层架构的职责划分，能说明每个模块为何如此切分。
- 对 AI 生成的核心代码（尤其 `qubit.measure` 与 `parties.Eve.intercept`），
  我都对照物理含义确认无误后才保留。
